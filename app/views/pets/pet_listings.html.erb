

<div class="container">
  <div class="container d-none" data-controller="filter-pets">
  </div>
    <h1 class="mt-3">Your pets:</h1>
  <p><strong><%= @pets.size %></strong> pets:</p>

<div class="row">
    <% if @pets %>
      <% @pets.each do |pet| %>
      <div class="col-4 mb-4">
        <div class="card">
          <img class="card-img-top"src="<%= cl_image_path pet.photo.key %>" height="300px">
          <div class="card-body bg-light">
            <div class="card-title">
              <h3><strong><%= pet.name.capitalize %></strong></h3>
            </div>
            <p><%= pet.description.capitalize %></p>
            <div class="d-flex">
              <div>
                <i class="fa-solid fa-house"></i>
                <span><%= pet.postcode.upcase %></span>
              </div>
              <div class="ms-3">
                <i class="fa-solid fa-sterling-sign"></i>
                <span><%= sprintf('%.2f', pet.price) %> / day</span>
              </div>
              <div class="ms-auto ">
                <%= link_to "See more", pet_path(pet.id), class: 'btn btn-sm btn-primary' %>
              </div>
            </div>
          </div>
        </div>
      </div>
      <% end %>
    <% end %>
  </div>
</div>
